---
title: Gateway
description: Public discovery with Effect and explicit application ports
---

The gateway serves GraphQL wallpaper discovery and public contributor Profiles from its own OpenSearch projections. It never hydrates responses by synchronously querying another service.

## Application structure

The Gateway Catalogue context owns one application hexagon. Four capabilities expose a single public entry each: catalogue reads, projection updates, request admission, and availability. Application-owned interfaces and typed `Context.Service` keys declare their dependencies. Capability layers yield these services; the composition root selects implementations and shares scoped resources. Infrastructure clients remain inside adapters.

Effect 4 composes execution, dependency construction, validation, and tracing. Normal decisions remain discriminated results; catalogue and projection unavailability use tagged technical errors. Driving adapters translate them into safe GraphQL responses or broker retry decisions. Defects remain distinct from expected failures. Effect scopes release resources and running fibers on shutdown and failed startup. Effect Config and Schema resolve configuration once before accepting work; only the adapters that need secrets receive them.

Fastify callbacks enter a single managed runtime. A scoped fiber set supervises request work and propagates client cancellation. NATS consumes async iterables through Effect streams, stops intake before bounded draining, and leaves incomplete processing available for redelivery. OpenSearch bridges interruption into the SDK request abort operation. The executable starts telemetry before importing instrumented adapters, and owns signal handling and process exit through `NodeRuntime`.

When a collector is configured, traces, metrics, and SDK logs use explicitly owned OTLP HTTP exporters at that collector's `/v1/traces`, `/v1/metrics`, and `/v1/logs` endpoints. SDK log-exporter environment settings, including `OTEL_LOGS_EXPORTER`, do not override this configuration. Exports and connections have one-second deadlines, including collectors that keep sending an unfinished response. Cleanup gives SDK shutdown four seconds, then closes and awaits owned connections; it also runs after startup failure.

Fastify and Mercurius preserve the GraphQL schema, including cursor pagination, color preferences, nullable contributor batching, and media URLs. HTTP failures outside GraphQL use RFC 9457 Problem Details; GraphQL failures keep `errors` and applicable `data` with safe metadata in `extensions`.

Wallpaper pages default to 10 results. Both `first` and `last` must be integers between 1 and 100 when supplied; `first` selects the page size if both are present. The capability rejects unsupported sizes before cursor decoding or catalogue storage access.

`searchProfiles(query, first, after)` discovers contributors through current Handles, active aliases and Display names. Biography is excluded from discovery, and alias lists are never returned. Exact current Handles rank before prefixes and alias matches, followed by Display-name matches. Profile ID breaks ties, and cursors bind the normalized query and ranking position. Queries ignore surrounding whitespace, case and one optional leading `@`; pages default to 10 with a maximum of 50. Selecting a contributor filters wallpapers by its exact Profile ID.

Exact Handle lookup returns the Profile together with the requested Handle, canonical Handle and alias indicator. Alias routing stops at a scheduled expiry even before the expiry event reaches the projection; retained aliases without an expiry remain active. Reclaimed Handles resolve using the newest generation of the matched claim, independently of discovery ranking. Profile snapshots preserve authored Biography content and current picture identity while older events cannot restore superseded state.

GraphQL complexity admission uses the selected operation and its referenced fragments, GraphQL-coerced arguments and variable defaults, and the catalogue's resolved page size. Equivalent pagination expressed as literals, supplied variables, or variable defaults receives the same cost estimate; omitted and null page sizes use the catalogue default.

Depth and cost follow executable field positions through fragments and honor `@skip` and `@include`. Repeated fields with the same response key merge their selections; aliases and different parent positions are charged separately. Nested list sizes multiply descendant costs, while connection metadata is counted once per connection. Analysis stops when a configured budget is exceeded, before catalogue work begins.

`GRAPHQL_MAX_COMPLEXITY` defaults to 2,000. The web application's 20-wallpaper browse query costs 1,506 with descendant work included, so it remains admitted under the default. The depth limit remains 5, and nested page expansion remains rejected. Explicit complexity settings continue to take precedence; deployments retaining the earlier setting of 1,000 must increase it to admit this browse query.

Successful `/health` and `/ready` responses and their HTTP status codes are unchanged. Failed probes return HTTP 503 with `application/problem+json`. Failed `/health` bodies use numeric `status: 503` and report the health state (`degraded`, `unhealthy`, or `shutting_down`) in `healthStatus`, alongside dependency checks and timing. Failed `/ready` bodies retain `ready: false`, `reason`, and `timestamp` within the problem envelope. Consumers of the former failed-health string `status` must read `healthStatus`; probes that only inspect HTTP status remain compatible.

## Projection delivery

Each OpenSearch projection mutation is atomic for one target. Uploads preserve previously received enrichment, duplicate variants do not accumulate, and an incomplete wallpaper remains hidden until its upload arrives. Profile versions never move backward. Complete legacy color/rendition snapshots converge by occurrence time and identity, since their producers do not yet provide entity versions.

Existing color projections lack occurrence metadata, so the first stale event after upgrade can temporarily replace their color data. Deployments requiring preserved historical color ordering must replay the complete retained color history before reopening catalogue traffic.

Occurrence ordering preserves the full fractional timestamp precision and normalizes equivalent UTC and numeric-offset timestamps to the same instant. Existing millisecond ordering keys remain comparable; public `updatedAt` values retain millisecond precision. Fractions already discarded by older gateway versions cannot be recovered from projection metadata. If retained events used finer precision, replay their complete history to restore precise ordering.

The broker adapter accepts structured and binary CloudEvents 1.0 and translates legacy producer envelopes at intake. Binary headers preserve the producer's occurrence identity; conflicting body and header identities are rejected. Replacing other services' contracts and retained events is a separate cross-application migration. Projection failures retry with finite exponential delays. Invalid, rejected, or exhausted deliveries are published to a durable quarantine stream before acknowledgment; quarantine outages keep delivery unacknowledged. Quarantine records preserve the original payload in `data.original` and complete NATS headers in `data.originalHeaders`, both base64 encoded. Replay restores both fields, including binary occurrence identity and trace context. Records follow the existing retained-event lifecycle described in the Profile projection ADR and issue #162.

`WALLPAPER` and `PROFILE` admit messages up to 64 KiB, including NATS headers. This applies to every publisher using their `wallpaper.>` and `profile.>` subjects. Infrastructure creates streams with this limit; gateway startup lowers unlimited or larger existing limits while preserving stricter limits. Quarantine requires at least 256 KiB per message, with the same minimum for the broker's `max_payload`, to leave room for base64, copied event identities and tracing headers. Incompatible broker or quarantine limits fail startup before subscriptions begin.

If copying header values into diagnostic JSON would exceed the quarantine budget, the record omits those duplicate diagnostic fields. Its original payload and complete headers remain available for replay.

Lowering a stream limit does not rewrite retained messages. Before first consumption, the gateway checks retained payload and original header bytes and records an audit certificate in the stream's `wallpaperdb.gateway.quarantine-budget` metadata. The certificate identifies the stream creation time, configured limit, policy version and audited sequence. Policy version 2 replaces the payload-only audit; streams with version 1 certificates are audited again. Matching certificates let subsequent replicas and restarts skip the history scan. The initial audit reads one retained message at a time through a fixed sequence snapshot; startup work is proportional to that history. Stream recreation or a changed limit requires another audit.

An oversized historical message stops startup with its stream, sequence and byte count; the original remains stored and unacknowledged. Export the identified record (for example, `nats stream get WALLPAPER <sequence> --json`), decide its retention and replay requirements, then explicitly resolve that record before restarting. Administrative changes that loosen a source limit must remove its audit certificate before accepting larger publications, including when restoring the old limit later. Gateway startup performs this invalidation when it repairs a larger limit itself. Changing the event-size contract requires revisiting quarantine capacity and the audit policy together.

Quarantine publication IDs fingerprint the broker domain/account, source stream, consumer responsibility, subject, original stream sequence and stored timestamp, and original bytes. Consumer retries preserve this identity; recreating a source stream does not conflate its new messages with older quarantine records. The broker timestamp is used as supplied by the NATS client, without first truncating it to an ISO date.

## Verification

```sh
make build PACKAGE=gateway
make check-types PACKAGE=gateway
make lint PACKAGE=gateway
make test-integration PACKAGE=gateway
```

Use `make test-focused PACKAGE=gateway ARGS='test/catalogue.test.ts'` for a focused behavioral suite. This builds prerequisites and runs one worker. Capability tests use controlled adapters; transport contracts use Fastify injection; storage and broker contracts use real containers. Integration tests check a representative publication-to-query path and lifecycle behavior.

The search test project owns one OpenSearch server per invocation. Each fixture owns unique wallpaper and Profile indices, its application, and its broker. Cleanup deletes only that fixture's indices, including closed indices; focused tests start only their required infrastructure. NATS readiness uses its health endpoint and bounded protocol requests.

Full suites use two worker threads and reuse loaded modules while fixtures own mutable application state. The OpenTelemetry bootstrap test runs in an isolated process because SDK shutdown leaves module instrumentation installed. Shuffled file/test runs verify order independence. Use `make test-focused PACKAGE=gateway ARGS='--coverage'` to run the coverage suite serially and reduce worker memory at the cost of elapsed time.

Coverage includes every gateway source file and writes reports to `apps/gateway/coverage/integration`, matching the shared repository analyzer's input layout. The measured pre-migration minimums are 89.72% statements and lines, 87.50% branches, and 96.74% functions. The dependency-graph check enforces capability entry points, inward dependencies and acyclicity. The gateway-specific CRAP checker has been removed. [Issue #217](https://github.com/rafaeltab/wallpaperdb/issues/217) tracks callback and generator coverage in the shared repository analyzer; no gateway CRAP score is claimed until that tooling is corrected.

## Operational notes

The executable emits Effect logs as JSON. Startup failures retain structured dependency, operation, error classification and available HTTP status or listener port. Retained-message audit failures include stream, sequence, payload and header byte counts, the applicable limit, and remediation. These diagnostics are available without a telemetry collector. Adapters supply allowlisted fields; raw vendor messages, connection URLs, credentials, response bodies and retained message contents are excluded. Unknown failures receive a fixed classification rather than serializing an arbitrary cause.

Quota enforcement fails open when Redis is disabled, unavailable, a command fails, or an instance's 64 concurrent command slots are occupied. Local saturation admits requests without enforcement even when Redis is healthy. The client reconnects; recovered storage and available command slots resume distributed limits. The `admission.quota.unenforced` counter distinguishes `disabled`, `unavailable`, `saturated`, and `command_failure` through its `reason` attribute, and the provisioned alert covers every reason. Commands have client deadlines, offline queuing and automatic resend are disabled, and stalled connections are closed. A timed-out quota mutation may already have committed; it is never retried within the request or charged to a second quota store. `COLOR_SPREAD_STRATEGY` is validated at startup. Invalid configuration fails before the gateway opens a listener. Broker connection and storage calls have bounded deadlines, and GraphQL and projection spans join the incoming OpenTelemetry trace. Transport cancellation cannot undo a committed projection; redelivery retains the original replay identity.

`OPENSEARCH_INDEX` and optional `OPENSEARCH_PROFILE_INDEX` select the wallpaper and Profile indices. Their defaults remain `wallpapers` and `profiles`.
