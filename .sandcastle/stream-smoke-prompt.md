# Sandcastle streaming smoke test

Perform exactly these steps, in order:

1. Say `hey from the Sandcastle streaming smoke test` to the user.
2. Use the bash tool to run `printf 'sandcastle-stream-command-ok\n'`.
3. Use the file-reading tool to open `.sandcastle/CODING_STANDARDS.md`.
4. Say `sandcastle-stream-smoke-complete` to the user.
5. Output `<promise>COMPLETE</promise>`.

Do not edit files, create commits, access GitHub, or perform any implementation work.
