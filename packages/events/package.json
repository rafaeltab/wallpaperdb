{
  "name": "@wallpaperdb/events",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Public event contracts for WallpaperDB services",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    },
    "./schemas": {
      "import": "./src/schemas/index.ts",
      "types": "./src/schemas/index.ts"
    }
  },
  "scripts": {
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "test": "vitest run",
    "test:unit": "vitest run --coverage",
    "test:watch": "vitest",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.1",
    "nats": "^2.29.1",
    "@opentelemetry/api": "^1.9.0",
    "ulid": "^2.3.0"
  },
  "peerDependencies": {
    "@wallpaperdb/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "@biomejs/biome": "^2.3.8",
    "@wallpaperdb/vitest-config": "workspace:*",
    "vitest": "^3.0.0"
  }
}
