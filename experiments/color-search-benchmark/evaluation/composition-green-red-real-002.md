# Natural-image follow-up: 40% green, 40% red, remainder unspecified

Status: **user judged in batch 001: A > B > D > C**, with the user's whole-batch quick-review caveat. Related development comparison using the same four photographs as [the 50/50 case](composition-green-red-real-001.md).

[Open review](http://zerotwo:8221/evaluation/composition-green-red-real-002.html) · [Machine-readable record](composition-green-red-real-002.json)

## Submitted judgment

The user submitted **A > B > D > C** with an empty case-specific note. Stable image order: `evaluation-green-red-leaves` > `wallpaper-082` > `evaluation-green-red-tulips` > `evaluation-green-red-tulip-field`.

The response is case 1 in submission `30b36a99-4c94-4f15-9e18-27373dea04c5`, saved at `2026-09-19T22:42:19.251Z`. It is one logical observation shared by this record and [the batch results](batch-001-results.md), not two independent annotations.

The user subsequently qualified the entire batch:

> I have submitted the answers, I went quite quickly, so dont regard this as 100% accurate human perception preference, some of these will be ranked very differently by different people

Keep the submitted order and this caveat together. No per-image descriptions, observed coverage, numeric confidence or ideal-match assessment were supplied. The difference from the previous 50/50 ordering does not isolate the effect of allowing an unspecified remainder.

## Query and purpose

**40% green, 40% red; the remaining 20% is unspecified.** These are targets for the whole image, not minimums or a normalized 50/50 split. No exact shade or subject is specified.

This adapts the previously authorized partial-composition direction to photographs after the user questioned the realism of pure-color bands. The separate stripe follow-up remains deferred. The prior natural-photo order and descriptions belong to their original query; no relevance judgments are copied into this case.

## Presentation and evidence

- Same four source files, checksums and complete-image `object-fit: contain` presentation.
- Fresh shuffled A–D mapping, stored in JSON and stable on refresh. Original stable image IDs and previous label mappings are preserved for traceability.
- No candidate percentages, measurements, previous judgments or anticipated ordering shown.
- Ask for brief impressions and a preferred order; ties, uncertainty or none fitting well are welcome.
- No candidate is expected to improve. An unchanged ranking is also useful evidence.

Both requested amounts and the unspecified remainder change from the previous query. A ranking change cannot be attributed exclusively to remainder flexibility. Reusing photographs reduces differences between scenes across queries, but recognition and prior exposure remain possible; a shuffle does not make this independent or fully blind evidence.

Keep this case, the earlier natural-photo case and related composition examples in the same development group. Record the response verbatim, and separate amount observations, perceived color strength, whole-image preference and explicit mismatch. No scoring weights, hard eligibility rules or numerical score gaps are predetermined.

Source attribution is on the review page and in the JSON. The files remain in the [shared image library](../CORPUS-STORAGE.md), unchanged. No search method, evaluation harness, benchmark or OpenSearch change accompanies this review.
