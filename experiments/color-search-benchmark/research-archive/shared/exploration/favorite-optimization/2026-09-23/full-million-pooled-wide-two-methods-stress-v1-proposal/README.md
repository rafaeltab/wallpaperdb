# Conditional two-method broad-bank comparison

Prepared only; no tests, service requests, runtime edits, or launch. The earlier bounded-only plan is preserved unchanged.

Compare `favorite-utility-numeric-docvalues` with `favorite-utility-bounded-pooled-delete`, using their exact existing primary candidate objects and the same complete full1M index. No presumed winner or maxima candidate.

Root must first verify both candidates' complete rate16 results, zero strict failures/rejections, successful6,138/6,138 utility coverage, and the accepted independent audit of the finished wide16 campaign. The pipeline itself does not implement that launch decision.

The existing runner accepts this subset and keeps original C1 qualification, recursive source/query bindings, allwide-query preflight,64selected warmups, and actual coverage accounting. It runs numeric then bounded, each at64 and128 requests/second for180seconds/rate; any strict64 failure omits that candidate's128 rate. One final independent audit follows.

Per candidate the scheduled counts are11,520 and23,040, each longer than the8,184-query cycle. Maximum timed work is12minutes across both candidates, plus preflight/warmup/settling/audit. Preserve cache/order and shared-host caveats.

Use fresh pipeline directory `full-million-pooled-wide-two-methods-stress-pipeline-v1` and output `full-million-pooled-wide-two-methods-stress-v1`. See `stress-plan.json` and `proposal-manifest.json` for exact arguments and file pins.
