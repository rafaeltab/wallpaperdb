meta {
  name: Readiness Check
  type: http
  seq: 2
}

get {
  url: http://localhost:3003/ready
  body: none
  auth: none
}

docs {
  Check if the media service is ready to handle requests.

  Returns 200 if ready, 503 if not ready (e.g., shutting down or connections not initialized).

  Used for Kubernetes readiness probes.
}
