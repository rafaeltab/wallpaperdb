# OpenCode uses the host OAuth subscription credentials mounted from
# ~/.local/share/opencode into the sandbox. OPENCODE_API_KEY is optional.
OPENCODE_API_KEY=

# GitHub token used by `gh issue ...` inside the sandbox.
# Required repository permissions: Issues (read/write) and Metadata (read).
GH_TOKEN=
