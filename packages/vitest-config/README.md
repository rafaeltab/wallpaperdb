# Shared Vitest configuration

Keeps workspace test output and coverage conventions consistent. Workspaces using a different Vitest major should import the runtime-independent `defaults` entry point and compose it with their own Vitest configuration.
